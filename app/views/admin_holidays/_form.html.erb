<%= form_for(@holiday) do |f| %>
  <% if @holiday.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@holiday.errors.count, "error") %> prohibited this holiday from being saved:</h2>

      <ul>
      <% @holiday.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <ol>
      <li><%= f.radio_button :status, "Pending" %> Pending</li>
      <li><%= f.radio_button :status, "Confirmed" %> Confirmed</li>
      <li><%= f.radio_button :status, "Canceled" %> Canceled</li>
    </ol>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>

<% end %>
