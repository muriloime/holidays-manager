%h1 New User
= form_for @user, :url => admin_users_path(@user)  do |f|
  - if @user.errors.any?
    #error_explanation
      %h2
        = pluralize(@user.errors.count, "error")
        prohibited this holiday from being saved:
      %ul
        - @user.errors.full_messages.each do |message|
          %li= message
  %p
    = f.label :name
    %br/
    = f.text_field :name
  %p
    = f.label "Login (must to be an email)"
    %br/
    = f.text_field :login
  .field
    %p
      = f.label :manager
      = f.radio_button :manager, FALSE
      FALSE
      \#{f.radio_button :manager, TRUE} TRUE
  .actions
    = f.submit class: 'btn_submit'
= link_to 'Back', admin_users_path, class:'btn_link'